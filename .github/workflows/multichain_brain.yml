name: multichain_brain
# MULTICHAIN_BRAIN: il cervello+loop+autolearn su TUTTE le chain. Ogni giorno impara (walk-forward onesto)
# su Solana/BSC/Base/Robinhood, scrive MULTICHAIN.md (quale chain rende di piu' + edge selezione). €0.
on:
  workflow_dispatch: {}
  schedule:
    - cron: "20 7 * * *"
permissions:
  contents: write
concurrency:
  group: multichain_brain
  cancel-in-progress: false
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - run: python agents/multichain_brain.py || echo "ko"
      - name: Salva
        run: |
          git config user.name "whale-radar-bot"; git config user.email "bot@users.noreply.github.com"
          git add MULTICHAIN.md 2>/dev/null || true
          git commit -m "multichain_brain $(date -u +%Y-%m-%dT%H:%MZ)" || { echo niente; exit 0; }
          for i in 1 2 3; do git pull --no-rebase --no-edit -X ours origin main || true; git push origin main && exit 0; sleep 4; done
